[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tree_diagram"
version = "0.1.0"
description = "Tree Diagram subsystem"
requires-python = ">=3.10"
